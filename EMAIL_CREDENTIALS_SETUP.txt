🚨 EMAIL ERROR SOLUTION - ईमेल त्रुटि समाधान 🚨

Current Error: "Application-specific password required"
समस्या: Gmail नियमित पासवर्ड स्वीकार नहीं कर रहा है

SOLUTION - समाधान:

1. CREATE .env FILE (.env फ़ाइल बनाएं):
   Create a file named ".env" in the server folder with this content:
   server फोल्डर में ".env" नाम की फ़ाइल बनाएं:

   GMAIL_USER=jrkwrit53@gmail.com
   GMAIL_APP_PASSWORD=YOUR_16_CHAR_APP_PASSWORD
   MONGODB_URI=mongodb://localhost:27017/ayodhya-court
   PORT=5000

2. GET GMAIL APP PASSWORD (Gmail App Password प्राप्त करें):
   
   STEP 1: Go to https://myaccount.google.com/
   STEP 2: Click "Security" → "2-Step Verification" → Turn ON
   STEP 3: Go to "Security" → "App passwords"
   STEP 4: Select "Mail" → "Other" → Name: "Ayodhya Court"
   STEP 5: Click "Generate" → Copy the 16-character password
   
   Example: abcd efgh ijkl mnop

3. UPDATE .env FILE (.env फ़ाइल अपडेट करें):
   Replace YOUR_16_CHAR_APP_PASSWORD with the actual password
   YOUR_16_CHAR_APP_PASSWORD को वास्तविक पासवर्ड से बदलें

4. RESTART SERVER (सर्वर रीस्टार्ट करें):
   Stop the server (Ctrl+C) and start again:
   सर्वर बंद करें (Ctrl+C) और फिर से शुरू करें:
   cd server
   node index.js

5. TEST EMAIL (ईमेल टेस्ट करें):
   Visit: http://localhost:5000/email-config/test
   
   Expected: "Email configuration: Ready to send emails"

QUICK FIX - त्वरित समाधान:
- Regular password (COLLERATEOFFICE@12345) won't work
- You MUST use Gmail App Password
- Enable 2-factor authentication first
- Generate App Password from Google Account settings

After following these steps, email will work properly!
इन चरणों के बाद, ईमेल सही तरीके से काम करेगा!
